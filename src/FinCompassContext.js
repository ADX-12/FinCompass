import React from "react";

/* Shared ThemeContext — exported here so sub-components can import it
   without creating a circular dependency with the giant FinCompass.jsx */
export const ThemeContext = React.createContext({});
